---
apiVersion: v1
kind: Service
metadata:
  name: immich-lb
  annotations:
    io.cilium/lb-ipam-ips: 10.10.2.9
spec:
  type: LoadBalancer
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    cnpg.io/cluster: immich
    role: primary

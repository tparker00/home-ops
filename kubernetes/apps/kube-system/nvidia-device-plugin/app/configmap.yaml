apiVersion: v1
kind: ConfigMap
metadata:
  name: nvidia-plugin-config
  namespace: kube-system
data:
  config.yaml: |
    sharing:
      timeSlicing: {}
    resources:
      gpus:
        - name: skeelz.net/gpu  # Custom unified name
          devices: all

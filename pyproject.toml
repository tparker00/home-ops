[project]
name = "home-ops"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
  "cloudflare==5.7.0",
  "email-validator==2.3.0",
  "makejinja==2.8.2",
  "netaddr==1.3.0",
]

[tool.uv]
package = false
